{
  "name": "healthapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.ts",
    "dev": "concurrently \"tsc --watch\" \"node --watch index.ts\"",
    "calculateBmi": "tsc --noEmit && node bmiCalculator.ts",
    "exerciseCalculator": "tsc --noEmit && node exerciseCalculator.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.1.1",
    "concurrently": "^10.0.3",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
